using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

//Dieses Script liegt nur im Prefab für die FotoContainer und ist notwendig, damit die pictures zugewiesen werden können.
public class FotoContainerScript : MonoBehaviour
{
    public RawImage rawImage;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
